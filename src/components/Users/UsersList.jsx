import React from 'react';
import { Box, Typography } from '@mui/material';

const UsersList = () => {
  return (
    <Box>
      <Typography variant="h3">Working with GET request</Typography>
    </Box>
  );
};

export default UsersList;
